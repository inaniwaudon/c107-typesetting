# columns-test

二段組の段分割／ページ分割のアルゴリズムに関する実装です。

- `index1.ts`：文章のみ
- `index2.ts`：文章 + 図版
- `index3.ts`：文章 + 図版、文章を優先して流し込む
- `index4.ts`：文章 + 図版、図版を上部に寄せる
- `index5.ts`：文章 + 図版、段抜き処理（二段組と一段組の混在）を含む

```bash
yarn
yarn run
yarn run compile
```
